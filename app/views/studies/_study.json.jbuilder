json.extract! study, :id, :legacy_id, :studynum, :title, :medium, :icpsr_num, :url, :directory, :folder, :r_flag, :timestamp, :auto_refresh, :note, :roper, :voyager_id, :created_at, :updated_at
json.url study_url(study, format: :json)